{
  "name": "procept-website",
  "version": "1.0.0",
  "description": "Site Procept - constructeur de maisons avec interface admin",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "sync": "node scripts/sync-content.js",
    "seo": "node scripts/sync-content.js && node scripts/generate-seo.js",
    "optimize-images": "node scripts/optimize-images.js",
    "build": "npm run sync && npm run optimize-images && npm run seo"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
